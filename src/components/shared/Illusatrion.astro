---
interface Props
{
    imageUrl : string;
    alt?: string;
}

const {imageUrl, alt = "Photographie ou illustration narrative"} = Astro.props;
---
<div class="relative rounded-3xl overflow-hidden shadow-2xl">
    <img class="w-full h-full object-cover" src={imageUrl} alt={alt}/>
</div>
